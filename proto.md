# 전국불판 PRD

## 한 줄 정의

> 지금 내가 있는 이 자리에서만 불을 지를 수 있는, GPS 기반 전국 실시간 스트레스 해소 웹 서비스

---

## 배경 및 문제 정의

흡연자들은 스트레스 받을 때 "담배 타임"이라는 합법적인 이탈 시간이 있다.
비흡연자에게는 그런 시간이 없다.

온라인 담타가 화제가 된 이유는 바로 이 박탈감을 건드렸기 때문이다.
전국불판은 같은 감각을 더 넓은 타겟에게, 더 시각적으로 강렬하게 제공한다.

```
스트레스 받는 순간
   ↓
내가 지금 있는 이 자리에 불을 지름
   ↓
전국 지도에서 같은 순간 불을 지르는 사람들이 보임
   ↓
혼자가 아니라는 느낌 + 집단적 카타르시스
```

---

## 핵심 컨셉

### GPS 기반 불 지르기

- 내가 지금 물리적으로 있는 위치에만 불을 지를 수 있음
- 집에서 강남 불 못 지름
- 실제로 그 자리에 있어야만 가능한 구조
- 담타와 동일한 "현장감" 부여

### 그리드 분할

읍/면/동 행정구역이 아닌 **고정 크기 격자**로 전국 지도를 분할

```
위도 0.001도 = 약 111m
경도 0.001도 = 약 88m (한국 기준)

격자 크기: 위도 0.001 x 경도 0.001
→ 약 100m x 100m 크기
→ 건물 1~3개 수준의 세밀함
→ "우리 회사 건물이 불타고 있다"는 느낌
→ 불 지른 곳만 Redis에 저장 (전국 수십만 격자 중 활성화된 것만)
```

### 불의 단계

같은 격자에 활성화된 불 지르기가 많을수록 불이 커짐

| 단계  | 활성 인원  | 시각 효과 |
| ----- | ---------- | --------- |
| 1단계 | 1~5명      | 불씨      |
| 2단계 | 6~20명     | 모닥불    |
| 3단계 | 21~50명    | 화재      |
| 4단계 | 51~100명   | 대형화재  |
| 5단계 | 100명 이상 | 전소      |

### 시간 기반 불 관리 (핵심)

각 불 지르기 이벤트는 **독립적인 30분 TTL**을 가짐

```
10:00 A가 불 지름 → 10:30까지 유효
10:10 B가 불 지름 → 10:40까지 유효
10:40 C가 불 지름 → 11:10까지 유효
11:00 D가 불 지름 → 11:30까지 유효
```

시간대별 활성 인원:

```
10:00 ~ 10:10 → 1명 (A)
10:10 ~ 10:30 → 2명 (A, B)
10:30 ~ 10:40 → 1명 (B)
10:40 ~ 11:00 → 1명 (C)
11:00 ~ 11:10 → 2명 (C, D)
11:10 ~ 11:30 → 1명 (D)
```

30분 TTL이 적당한 이유:

- 1시간이면 너무 오래 유지돼서 실시간 긴장감 떨어짐
- "불 꺼지기 전에 한 번 더 질러야지" 하는 재방문 유도 가능
- 겹치는 사람이 많을수록 불이 유지되는 구조

### 소방관 메커니즘

불이 일정 단계 이상 되면 소방차가 출동해서 불을 끔

```
4단계 이상 → 소방차 1대 출동
5단계 이상 → 소방차 3대 동시 출동
   ↓
물 뿌리는 애니메이션
불 단계 강제로 낮춰짐
   ↓
근데 사람들이 계속 지르면 다시 올라감
   ↓
소방관 vs 불지르는 사람들 실시간 줄다리기
```

채팅방에서 자연스럽게 "다같이 지르자" 하는 집단행동 유도

---

## 기능 명세

### 1. 메인 지도 화면

- OpenStreetMap 기반 전국 지도
- 100m x 100m 고정 격자로 분할
- 활성 격자만 실시간 렌더링 (불 없는 격자는 표시 안 함)
- 불 단계별 시각 효과 (색상, 애니메이션 강도)
- 내 현재 위치 표시
- 전국 동시 불 지르는 사람 수 실시간 카운터

### 2. 불 지르기

- 현재 위치 GPS 감지 → 100m 격자 매핑
- 내 위치 격자에만 불 지르기 가능
- 화면 꾹 누르면 불씨 커지는 애니메이션
- 떼는 순간 불 생성 효과음 + 시각 효과
- 불 지른 시점부터 30분 TTL 독립 적용
- 횟수 제한 없음 (계속 지를 수 있음)

### 3. 소방관 시스템

- 4단계 이상 격자에 소방차 자동 출동
- 물 뿌리는 애니메이션 주기적 실행
- 소방차 활성화 중 불 단계 감소 속도 빨라짐
- 사람들이 계속 지르면 소방차 무력화 후 단계 재상승

### 4. 지역 채팅

- 현재 내 격자 기준 반경 채팅방 자동 입장
- 완전 익명
- 현재 불 지르는 사람 수 실시간 표시
- 채팅 내용 24시간 후 자동 삭제

### 5. 히트맵 & 통계

- 오늘 전국 불 현황 실시간
- 시간대별 히트맵
- 일별 / 월별 아카이브
- 역대 제일 많이 불탄 격자 TOP 10
- 지금 이 순간 전국 동시 불 지르는 사람 수

---

## 핵심 시나리오

### 시나리오 1 — 오후 2시 회의 직후

```
지옥 같은 2시간 회의 끝
자리로 돌아오는 길에 폰 꺼냄
   ↓
전국불판 접속
내 회사 위치에 불 지르려는 순간
   ↓
"어? 이미 누가 불 질렀네"
우리 회사 격자에 이미 모닥불
   ↓
"ㅋㅋㅋㅋ 나만 힘든 게 아니었어"
채팅방 "방금 회의 누구야 ㅋㅋ"
나도 불 지름 → 화재로 올라감
```

### 시나리오 2 — 처음 접속한 순간

```
친구한테 링크 받아서 접속
   ↓
전국 지도 펼쳐지는데
강남 테헤란로 일대 이미 불바다
   ↓
"ㅋㅋㅋㅋ 불장난 아니네"
캡처해서 인스타 스토리 올림
"회사원들 ㄹㅇ 힘든가봄 ㅋㅋ"
   ↓
스토리 보고 친구들도 접속
```

### 시나리오 3 — 소방관 출동

```
우리 회사 격자 4단계 돌입
소방차 등장 물 뿌리기 시작
   ↓
채팅방 "소방관 왔다 ㅋㅋㅋ"
"다같이 질러서 이겨보자"
   ↓
동시에 10명이 불 지름
소방차 무력화 → 5단계 전소
   ↓
"우리가 이겼다 ㅋㅋㅋㅋ"
전소 화면 캡처해서 스토리 공유
```

### 시나리오 4 — 발표 데모

```
발표장에서 QR 코드 뿌림
   ↓
청중 다같이 접속
각자 현재 위치에 불 지름
   ↓
발표장 건물 격자 실시간 불타오름
소방차 출동
청중이 소방차 이기려고 계속 누름
   ↓
발표 끝
```

---

## 바이럴 구조

캡처 공유가 자연스럽게 일어나는 구조. 별도 공유 버튼 없어도 됨.

```
강남 테헤란로 불바다 지도 캡처
   ↓
"우리 회사 불탔다 ㅋㅋ" 인스타 스토리
   ↓
스토리 보고 친구들 접속
   ↓
자기 회사도 불 지름
   ↓
더 넓은 지역이 불바다
   ↓
더 강렬한 캡처 → 더 많은 공유
```

---

## 기술 스택

```
프론트엔드
- OpenStreetMap + Leaflet.js (지도)
- Canvas API (불 + 소방차 애니메이션)
- Socket.io 클라이언트 (실시간 동기화)

백엔드
- Node.js + Express
- Socket.io (WebSocket)

데이터베이스
- Redis Sorted Set: 격자별 불 이벤트 + TTL 관리
- PostgreSQL: 히트맵 통계 저장

위치
- Web Geolocation API
```

---

## 핵심 로직

### 그리드 계산

```javascript
function getGridId(lat, lng) {
  const gridLat = Math.floor(lat / 0.001);
  const gridLng = Math.floor(lng / 0.001);
  return `${gridLat}:${gridLng}`;
}
```

### 불 이벤트 저장 (Redis Sorted Set)

```
key: fire:{gridId}
score: 만료 시각 (unix timestamp)
value: 이벤트 고유 ID

불 지르기  → ZADD fire:{gridId} {now+1800} {eventId}
활성 카운트 → ZCOUNT fire:{gridId} {now} +inf
만료 정리  → ZREMRANGEBYSCORE fire:{gridId} 0 {now}
```

### 단계 계산

```javascript
function getFireStage(activeCount) {
  if (activeCount >= 100) return 5; // 전소
  if (activeCount >= 51) return 4; // 대형화재
  if (activeCount >= 21) return 3; // 화재
  if (activeCount >= 6) return 2; // 모닥불
  if (activeCount >= 1) return 1; // 불씨
  return 0;
}
```

### 소방관 트리거

```
격자 단계 4 이상 감지
   ↓
소방차 출동 이벤트 WebSocket 브로드캐스트
   ↓
30초마다 해당 격자 활성 카운트 감소 (소방 효과)
   ↓
격자 단계 2 이하로 내려가면 소방차 철수
사람들이 계속 지르면 다시 올라가서 소방차 무력화
```

### 실시간 동기화

```
불 지르기 이벤트 발생
   ↓
Redis 업데이트
   ↓
해당 격자 주변 클라이언트에만 브로드캐스트
(전체 브로드캐스트 X → 뷰포트 안 격자만)
```

---

## 심사 기준 대응

| 기준   | 대응                                              |
| ------ | ------------------------------------------------- |
| 실용성 | 스트레스 해소 + 커뮤니티. 담타 화제성이 니즈 증명 |
| 창의성 | GPS 현장 제한 + 시간 기반 TTL + 소방관 줄다리기   |
| 완성도 | 지도 + 불 + 채팅 + 소방관 하루 안에 구현 가능     |
| 임팩트 | 발표장 라이브 데모 + 캡처 공유로 즉각 바이럴      |

---

## MVP 범위 (해커톤 당일)

- 현재 위치 GPS → 100m 격자 매핑
- 내 격자에만 불 지르기
- 불 단계별 시각 효과
- 30분 TTL 기반 실시간 단계 계산
- 소방관 출동 애니메이션
- 전국 지도 실시간 동기화 (WebSocket)
- 격자 기반 채팅방
- 오늘 히트맵

## 확장 포인트

- 불 종류 다양화 (작은 불씨 / 폭발 / 핵폭발 등)
- 격자별 역대 최고 기록 명예의 전당
- 주간/월간 리포트 자동 생성
- 소방관 강도 조절 (많이 질러서 이기면 소방관 포기 애니메이션)
- 특정 시간대 전국 동시 불 지르기 이벤트
